---
import ButtonLink from "../../ui/button/ButtonLink.astro";

interface Props {
	href: string;
	label: string;
	labelKey?: string;
	variant?: "default" | "outline" | "ghost" | "disabled";
}

const { ...props } = Astro.props as Props;
---

<ButtonLink {...props} icon="tabler:arrow-back-up" />
