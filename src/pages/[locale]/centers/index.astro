---
import CentersPage from "../../centers/index.astro";
import { defaultLocale, locales } from "@/i18n";

export function getStaticPaths() {
	return locales
		.filter((locale) => locale !== defaultLocale)
		.map((locale) => ({ params: { locale } }));
}
---

<CentersPage />
